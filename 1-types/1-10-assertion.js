"use strict";
{
    // Type Assertions
    function jsStrFunc() {
        return 'hello';
    }
    const result = jsStrFunc();
    console.log(result.length);
    const wrong = 5;
    console.log(object);
}
