"use strict";
{
    // Array
    const fruits = ['tomato', 'banana'];
    const scores = [1, 3, 4];
    function printArray(fruits) { }
    // Tuple
    let student;
    student = ['name', 123];
    student[0]; //name
    student[0]; //123
}
